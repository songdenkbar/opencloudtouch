# ==============================================================================
# TEST BUILD - Docker + Raspi images from any branch for testing
#
# Trigger: workflow_dispatch on any branch
# Purpose: Build & push images with a custom tag (e.g. "test", "bug-184")
#          so testers can pull and verify before merging.
#
# Docker images pushed to GHCR with the chosen tag ONLY (never "latest"/"stable").
# Raspi images uploaded as workflow artifacts (30 day retention).
# No version bump, no git tag, no GitHub Release.
#
# Set real_supporters=true to fetch the real supporters list from
# opencloudtouch.org instead of using the placeholder CSV committed to git.
# ==============================================================================

name: Test Build

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag (e.g. "test", "bug-184", "feature-xyz")'
        required: true
        type: string
      real_supporters:
        description: 'Fetch the real supporters list from opencloudtouch.org (default: placeholder CSV from git)'
        required: false
        type: boolean
        default: false
      docker_amd64:
        description: 'Docker: linux/amd64 (Desktop, Server, NAS)'
        required: false
        type: boolean
        default: true
      docker_arm64:
        description: 'Docker: linux/arm64 (Raspberry Pi 4/5)'
        required: false
        type: boolean
        default: false
      docker_armv7:
        description: 'Docker: linux/arm/v7 (Raspberry Pi 2/3)'
        required: false
        type: boolean
        default: false
      raspi_arm64:
        description: 'Raspi SD Image: arm64'
        required: false
        type: boolean
        default: false
      raspi_armhf:
        description: 'Raspi SD Image: armhf'
        required: false
        type: boolean
        default: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: songdenkbar/opencloudtouch

# Least-privilege: no default permissions. Jobs declare what they need.
permissions: {}

jobs:
  # ============================================================================
  # VALIDATE INPUTS
  # ============================================================================
  validate:
    name: Validate Inputs
    runs-on: ubuntu-latest

    outputs:
      docker_platforms: ${{ steps.platforms.outputs.docker_platforms }}
      has_docker: ${{ steps.platforms.outputs.has_docker }}
      raspi_archs: ${{ steps.platforms.outputs.raspi_archs }}
      has_raspi: ${{ steps.platforms.outputs.has_raspi }}

    steps:
      - name: Compute platform matrix
        id: platforms
        run: |
          # Docker platforms
          DOCKER_LIST=""
          if [ "${{ inputs.docker_amd64 }}" = "true" ]; then
            DOCKER_LIST="${DOCKER_LIST:+$DOCKER_LIST,}\"linux/amd64\""
          fi
          if [ "${{ inputs.docker_arm64 }}" = "true" ]; then
            DOCKER_LIST="${DOCKER_LIST:+$DOCKER_LIST,}\"linux/arm64\""
          fi
          if [ "${{ inputs.docker_armv7 }}" = "true" ]; then
            DOCKER_LIST="${DOCKER_LIST:+$DOCKER_LIST,}\"linux/arm/v7\""
          fi

          if [ -n "$DOCKER_LIST" ]; then
            echo "docker_platforms=[${DOCKER_LIST}]" >> "$GITHUB_OUTPUT"
            echo "has_docker=true" >> "$GITHUB_OUTPUT"
            echo "Docker platforms: [${DOCKER_LIST}]"
          else
            echo "docker_platforms=[]" >> "$GITHUB_OUTPUT"
            echo "has_docker=false" >> "$GITHUB_OUTPUT"
            echo "Docker platforms: none"
          fi

          # Raspi architectures
          RASPI_LIST=""
          if [ "${{ inputs.raspi_arm64 }}" = "true" ]; then
            RASPI_LIST="${RASPI_LIST:+$RASPI_LIST,}\"arm64\""
          fi
          if [ "${{ inputs.raspi_armhf }}" = "true" ]; then
            RASPI_LIST="${RASPI_LIST:+$RASPI_LIST,}\"armhf\""
          fi

          if [ -n "$RASPI_LIST" ]; then
            echo "raspi_archs=[${RASPI_LIST}]" >> "$GITHUB_OUTPUT"
            echo "has_raspi=true" >> "$GITHUB_OUTPUT"
            echo "Raspi architectures: [${RASPI_LIST}]"
          else
            echo "raspi_archs=[]" >> "$GITHUB_OUTPUT"
            echo "has_raspi=false" >> "$GITHUB_OUTPUT"
            echo "Raspi architectures: none"
          fi

      - name: Check at least one target selected
        run: |
          if [ "${{ steps.platforms.outputs.has_docker }}" = "false" ] && \
             [ "${{ steps.platforms.outputs.has_raspi }}" = "false" ]; then
            echo "::error::No build target selected. Check at least one architecture."
            exit 1
          fi

  # ============================================================================
  # BUILD FRONTEND
  # ============================================================================
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    needs: [validate]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: |
          npm ci
          npm install @rollup/rollup-linux-x64-gnu --save-optional

      - name: Fetch supporters from opencloudtouch.org
        if: inputs.real_supporters == true
        env:
          SUPPORTERS_API_USER: oct-ci
          SUPPORTERS_API_PASS: ${{ secrets.SUPPORTERS_API_PASS }}
        run: |
          # Fetch current supporters CSV via Basic Auth
          AUTH=$(echo -n "$SUPPORTERS_API_USER:$SUPPORTERS_API_PASS" | base64)
          curl -H "Authorization: Basic $AUTH" \
            https://opencloudtouch.org/api/supporters/get.php \
            -o apps/frontend/public/supporters.csv --fail --silent --show-error --remove-on-error || {
            echo "⚠️ Failed to fetch supporters, keeping placeholder CSV from git"
            git checkout -- apps/frontend/public/supporters.csv
          }
          LINES=$(wc -l < apps/frontend/public/supporters.csv)
          if [ "$LINES" -le 1 ]; then
            echo "::warning::Supporters fetch returned an empty list — keeping placeholder CSV from git"
            git checkout -- apps/frontend/public/supporters.csv
          fi
          echo "✅ Supporters: $(wc -l < apps/frontend/public/supporters.csv) lines"

      - name: Build frontend
        working-directory: apps/frontend
        run: npm run build

      - name: Upload frontend dist
        uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: .out/dist
          retention-days: 1

  # ============================================================================
  # DOCKER BUILD (per-platform)
  # ============================================================================
  build-docker:
    name: Build Docker (${{ matrix.platform }})
    runs-on: ubuntu-latest
    needs: [validate, build-frontend]
    if: needs.validate.outputs.has_docker == 'true'

    strategy:
      fail-fast: false
      matrix:
        platform: ${{ fromJSON(needs.validate.outputs.docker_platforms) }}

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download pre-built frontend
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: frontend-ctx/build/.out/dist

      - name: Set up QEMU
        if: matrix.platform != 'linux/amd64'
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set test build signature
        id: sign
        run: |
          echo "signature=test-build" >> "$GITHUB_OUTPUT"

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: deployment/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          build-args: |
            OCT_BUILD_SIGNATURE=${{ steps.sign.outputs.signature }}
          build-contexts: |
            frontend-build=frontend-ctx
          labels: |
            org.opencontainers.image.title=OpenCloudTouch
            org.opencontainers.image.description=Test build from ${{ github.ref_name }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=test-${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=test-${{ matrix.platform }}
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ strategy.job-index }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  # ============================================================================
  # DOCKER PUSH (merge digests into multi-arch manifest)
  # ============================================================================
  push-docker:
    name: Push Docker Manifest
    runs-on: ubuntu-latest
    needs: [validate, build-docker]
    if: needs.validate.outputs.has_docker == 'true'

    permissions:
      contents: read
      packages: write

    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          pattern: digests-*
          path: /tmp/digests
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          TAG="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }}"
          echo "Creating manifest for tag: $TAG"
          docker buildx imagetools create -t "$TAG" \
            $(printf '${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@sha256:%s ' *)

      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }}

  # ============================================================================
  # RASPI BUILD + TEST (per-arch, via reusable workflow)
  # ============================================================================
  build-raspi-arm64:
    name: Raspi arm64
    needs: [validate, build-frontend]
    if: contains(fromJSON(needs.validate.outputs.raspi_archs), 'arm64') && needs.build-frontend.result == 'success'
    permissions:
      actions: write
    uses: ./.github/workflows/raspi-build-and-test.yml
    with:
      arch: arm64
      output_name: 'opencloudtouch-arm64-${{ inputs.image_tag }}.img.xz'
      docker_image_tag: ${{ inputs.image_tag }}
      test_version: ${{ inputs.image_tag }}

  build-raspi-armhf:
    name: Raspi armhf
    needs: [validate, build-frontend]
    if: contains(fromJSON(needs.validate.outputs.raspi_archs), 'armhf') && needs.build-frontend.result == 'success'
    permissions:
      actions: write
    uses: ./.github/workflows/raspi-build-and-test.yml
    with:
      arch: armhf
      output_name: 'opencloudtouch-armhf-${{ inputs.image_tag }}.img.xz'
      docker_image_tag: ${{ inputs.image_tag }}
      test_version: ${{ inputs.image_tag }}

  # ============================================================================
  # SUMMARY
  # ============================================================================
  summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [validate, push-docker, build-raspi-arm64, build-raspi-armhf]
    if: always() && needs.validate.result == 'success'

    steps:
      - name: Step summary
        run: |
          TAG="${{ inputs.image_tag }}"
          BRANCH="${{ github.ref_name }}"
          SHA="${{ github.sha }}"

          cat >> $GITHUB_STEP_SUMMARY << 'HEADER'
          # Test Build Summary
          HEADER

          cat >> $GITHUB_STEP_SUMMARY << EOF

          | | |
          |---|---|
          | **Tag** | \`${TAG}\` |
          | **Branch** | \`${BRANCH}\` |
          | **Commit** | \`${SHA:0:7}\` |
          EOF

          # Docker section
          if [ "${{ needs.validate.outputs.has_docker }}" = "true" ]; then
            cat >> $GITHUB_STEP_SUMMARY << EOF

          ## Docker Image

          \`\`\`bash
          docker pull ghcr.io/${{ env.IMAGE_NAME }}:${TAG}
          \`\`\`

          \`\`\`bash
          docker run -d \\
            --name opencloudtouch-test \\
            --network host \\
            -v oct-test-data:/data \\
            -e OCT_DISCOVERY_ENABLED=true \\
            ghcr.io/${{ env.IMAGE_NAME }}:${TAG}
          \`\`\`
          EOF
          fi

          # Raspi section
          if [ "${{ needs.validate.outputs.has_raspi }}" = "true" ]; then
            cat >> $GITHUB_STEP_SUMMARY << EOF

          ## Raspberry Pi Images

          Download from the **Artifacts** section of this workflow run.
          Flash with Balena Etcher or: \`xzcat opencloudtouch-arm64-${TAG}.img.xz | sudo dd of=/dev/sdX bs=4M status=progress\`
          EOF
          fi
